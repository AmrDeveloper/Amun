fun integers(varargs int64) void {
    return;
}

fun floats(varargs float64) void {
    return;
}

fun anys(varargs Any) void {
    return;
}

fun main() int64 {
    integers();
    integers(1);
    integers(1, 2);

    floats();
    floats(1.0f64);
    floats(1.0f64, 2.0f64);

    anys();
    anys(1, 1.0f64);
    anys(2, 2);
    anys(3.0f64, 3.0f32);
    anys("Hello", "World");
    return 0;
}