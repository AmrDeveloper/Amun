@extern fun printf(format *char, value int64) int64;

fun main() int64 {
    var x = [1, 2, 3, 3, 4, 5];
    var i = 0;
    while (i < 6) {
        printf("%d", x[i]);
        i += 1;
    }
    return 0;
}