@extern fun printf(format *char, value int64) int64;

fun main() int64 {
    var matrix = [[1, 5, 3], [4, 5, 6], [7, 8, 9]];

    var i = 0;
    while (i < 3) {
        var j = 0;
        while (j < 3) {
           printf("%d", matrix[i][j]);
           j += 1;
        }
        i += 1;
    }

    return 0;
}