@extern fun printf(format *char, varargs Any) int64;

fun main() int64 {
    var sum = { (x int64, y int64) int64 -> return x + y; };

    printf("Sum of 10 and 20 = %d\n", sum(10, 20));
    return 0;
}