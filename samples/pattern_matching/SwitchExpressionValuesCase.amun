@extern fun printf(format *char, value int64) int64;

fun main() int64 {
    var x = 10;
    var y = 5;
    var i = 8;

    var value = switch (i) {
        0, 2, 4, 6, 8 -> 1;
        else -> 0;
    };

    printf("Is Even less than 10? %d\n", value);
    return 0;
}